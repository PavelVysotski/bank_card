server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://user-service-db:5432/user_service
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
